import { makeView } from "./_placeholder.js?v=v2-1";
const v = makeView({ name: "Menu", accent: "var(--ink,#eaf0fb)",
  blurb: "Search, recents, account & settings. Ported last, alongside the command palette and pull-to-refresh." });
export const mount = v.mount;
