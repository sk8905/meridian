import { makeView } from "./_placeholder.js?v=v2-1";
const v = makeView({ name: "Home", accent: "var(--brand-2,#fb8b1e)",
  blurb: "Glance landing (markets pulse, live wire, watchlist). Phase 0 shell — real content is ported here next. Switch tabs and come back: this loads once, then every return is instant." });
export const mount = v.mount;
